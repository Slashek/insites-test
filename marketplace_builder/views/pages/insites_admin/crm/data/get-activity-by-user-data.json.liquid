---
slug: api/get-activities-by-user-data.json
authorization_policies:
 - only_allowed_by_administrators
---

{% if params.id %}
    {% assign id = params.id | plus: 0  %}
{% endif %}
{% if params.size %}
    {% assign size = params.size | plus: 0 %}
{% endif %} 
{% if params.page %}
    {% assign page = params.page | plus: 0 %}
{% endif %}
{% if params.sortBy %}
    {% assign sortBy = params.sortBy | downcase %}
{% endif %}
{% if params.orderBy %}
    {% assign orderBy = params.orderBy | downcase %}
{% endif %}
{% if params.keyword %}
    {% assign keyword = params.keyword | downcase %}
{% endif %}
 
{% query_graph 'get_activities_by_user', result_name: 'data', id: id, keyword: keyword, size: size, page: page, sortBy: sortBy, orderBy: orderBy %}
{{ data | json }}
